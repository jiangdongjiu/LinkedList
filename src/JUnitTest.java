import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class JUnitTest
{
    /// <summary>
    /// This one test will pass and you should see a green check mark at the bottom left of the screen
    /// To run the test, Right-Click the file and choose run JUnit Test ...
    /// If this test is not working:
    /// hover over the red text in the import statement above and use: alt+shift+enter to install the missing JUnit library!
    /// You could also try using Visual Studio and C# instead. ;P
    /// </summary>
    @Test
    public void This_Test_Will_Always_Pass_Test()
    {
        assertTrue(true);
    }
}
